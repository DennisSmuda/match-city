generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model match_city_highscores {
  id    Int    @id @default(autoincrement())
  name  String
  score BigInt

  @@map("match-city-highscores")
}
